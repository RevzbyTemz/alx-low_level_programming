#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Alaways 0 (Success/Correct)
 */
int main(void)


{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar(upper_case);
}

for (lower_case = 'a'; lower_case <= 'z'; lower_case--)
{
putchar(alphabet);
}

putchar("\n");

return (10);
}
